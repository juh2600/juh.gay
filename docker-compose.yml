services:
  juh_gay:
    image: jtreed/semistatic-nginx
    environment:
      - REPO=https://github.com/josephreed2600/juh.gay.git
      - BRANCH=dev
      - FREQUENCY=* * * * *
      - DIR=doc
      - NGINX_CONF=nginx-server.conf
      - BUILD=build/build.fish
    ports:
      - 80:80
